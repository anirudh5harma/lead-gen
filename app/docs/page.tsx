import DocsClient from './DocsClient'

export const metadata = {
  title: 'API · Bombsell',
  description: 'GTM as tools your agent can call — discover agents, dispatch tasks, run pipelines, receive webhooks, manage keys. A2A · REST · MCP.',
}

export default function DocsPage() {
  return <DocsClient />
}
